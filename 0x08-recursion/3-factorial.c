#include "main.h"

/**
 * factorial - Returns the factorial of a given number
 *@n: the given number
 *
 * Return: 0 always
 */

int factorial(int n)
{
	if (n < 0)
		return (-1);
	return n * factorial(n = 1)
}

int main ()
{
	_putchar (n);

}
_putchar ('\n');
return (0);
